class PlotableSearch:
    def __init__(self):
        pass

    def iterativeSearch(self, x, data = []):
        raise NotImplementedError("Método de busca iterativa não foi implementado!")
    
    def recursiveSearch(self, x, data = []):
        raise NotImplementedError("Método de busca recursiva não foi implementado!")
    
    def toString(self): raise NotImplementedError("Método toString não implementado!")
